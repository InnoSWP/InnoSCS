# SCS

### Description:
    backend for scs

### Run in docker
    make docker

### Create venv:
    make venv

### Run API
    make up

### Run tests:
    make test

### Run linters:
    make lint

### Run formatters:
    make format
